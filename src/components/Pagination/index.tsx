import React from 'react';

const Pagination = ({ nextPageBtn, prevPageBtn }: any) => {
	return (
		<div>
			<button>Next</button>
			<button>Prev</button>
		</div>
	);
};

export default Pagination;
